services:
  mc-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - ${MC_SERVER_PORT}:25565/tcp
      - ${MC_SERVER_PORT}:25565/udp
    env_file:
      - .env
    volumes:
      - mc-server-data:/mc
    restart: unless-stopped

volumes:
  mc-server-data: